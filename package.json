{
  "name": "distributed-banking-system",
  "version": "1.0.0",
  "description": "A distributed banking system with React frontend, Node.js backend, and Ethereum smart contracts",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install && cd ../blockchain && npm install",
    "test": "cd server && npm test && cd ../blockchain && npm test"
  },
  "keywords": ["banking", "blockchain", "distributed", "react", "nodejs"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

